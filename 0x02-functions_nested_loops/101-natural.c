/**
* main - entry
* @i: int arg
* Return: sum of multiples of 3 and 5
*/
int main(int i)
{
	int sum;
	int i;

	sum = 0;
	for (i = 0; i <= 1024; i++)
	{
		m = i % 3;
		k = i % 5
		if (m == 0 || k == 0)
		{
			sum = sum + i;
		}
	}
	return (sum);
}
