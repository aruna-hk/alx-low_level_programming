#include <stdio.h>
/**
* print_diagums - print sum of diagonal
* @a: ptr to multidemensinal array
* @size: len 0f 1d arrays in multiD array;
*/
void print_diagsums(int *a, int size)
{
	int sum-diag1 = 0;
	int sum_diag2 = 0;
	int i;
	int len = 0;

	while (*a != '\0')
	{
		len++;
		a++;
	}
	while (i < size))
	{
		while
