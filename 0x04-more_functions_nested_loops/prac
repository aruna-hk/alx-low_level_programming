#include "main.h"
#include <stdio.h>

/**
* _isdigit - check
* Return: 1 if else 0
*/
int _isdigit(int c)
{
	if (i >= 48 && i<=57)
		return (1);
	else
		return (0);
}
int main()
{
	char c;
	c ='0';
	printf("%c: %d\n", c, _isdigit(c));
	return 0;
}
